 {% extends 'front.html.twig'%}
{% block body %}
<br>
<br>
<br>
<div class="container">
    <h1 class="highlight">Nos articles</h1>
</div>

    <div class="container container-fluid" style="padding: 10vh">
  <div class="row row-cols-1 row-cols-md-3 g-4">
  {% for produit in produits %}
  <div class="col">
    <div class="card h-100">
      <img src="/upload/{{produit.photo}}" class="card-img-top" style="width: 100%;
height: 70vh; object-fit: cover;" alt="#">
      <div class="card-body">
        <h5 class="card-title">{{produit.titre}}</h5>
        <p class="card-text">{{produit.description}}</p>
      </div>
      <div class="card-footer">
        <small class="text-muted">{{produit.prix}} €</small>
        <a href="{{ path ('home_add_panier' , {"id" : produit.id }) }}" 
        class="btn text-end p-2 text-white" 
        style="background-color:#C0E14F; margin-left: 150px">ajouter au panier</a>
    </div>
    </div>
  </div>
  {% endfor %}
  </div>
</div>
{% endblock %}

